# TokenConsumptionMonitoring

桌面额度监控小工具：常驻展示各 API 供应商的 token/额度用量，并提供页面化的配置与告警。

## Language

**桌面组件**:
常驻桌面的悬浮监控小窗，展示当前激活页面的用量概览。
_Avoid_: 悬浮窗（实现名）、小部件

**面板**:
点击桌面组件或托盘打开的配置窗口，用于管理页面与告警；下拉框默认选中第一份配置。
_Avoid_: 主窗口、配置页

**新建**:
面板中创建新页面的操作（原称"添加模型供应商"，已废弃）。
_Avoid_: 添加模型供应商、新增

**页面**:
一个 API 供应商的用量监控配置单元（Base URL、协议、密钥、模型列表）。
_Avoid_: 账号、标签页

**开机自启**:
系统登录时自动启动本应用的设置项。

**今日用量**:
zcode CLI 本地记录统计出的当日 token 消耗（全口径 = 含缓存读取），按 API key 归属到对应页面后在桌面组件展示，可分模型拆分。
_Avoid_: 日消耗、今日 token

**zcode 本地数据**:
zcode CLI 在本机的会话与用量存储（model-io 记录与 provider 配置），是今日用量的唯一数据源；区别于远程官方接口。
